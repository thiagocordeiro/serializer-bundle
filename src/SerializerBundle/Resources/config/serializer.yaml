parameters:
  serializer.cache_dir: '%kernel.cache_dir%'
  serializer.check_timestamp: false

services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: true

  Serializer\ClassFactory:
    arguments:
      $cacheDir: '%serializer.cache_dir%'
      $checkTimestamp: '%serializer.check_timestamp%'

  Serializer\JsonSerializer: ~
